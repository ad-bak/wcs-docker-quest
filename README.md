# Getting started

## How to run ?

### `docker compose -f docker-compose.dev.yml up --build`

Run this command in the root directory of the project. This will start the server and the client in development mode.
# wcs-docker-quest
